apiVersion: gateway.networking.minefleet.dev/v1
kind: MinecraftServerDiscovery
metadata:
  labels:
    app.kubernetes.io/name: minecraft-gateway
    app.kubernetes.io/managed-by: kustomize
  name: minecraftserverdiscovery-sample
spec:
  namespaceSelector:
    from: Same
  labelSelector:
      matchLabels:
        minefleet.dev/environment: sample
